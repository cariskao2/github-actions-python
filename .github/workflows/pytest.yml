name: pytest
on: workflow_dispatch

jobs:
  pytest:
    runs-on: ubuntu-latest
    steps:
			- name: checkout code # 也可以起名字
				uses: actions/checkout@v3 # 以上4行用這行代替
      - name: install pytest
        run: pip install pytest
      - name: run pytest
        run: pytest
